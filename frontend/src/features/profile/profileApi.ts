import { apiClient } from "@/shared/api";

export const profileApi = {
  getProfile: async () => {
    // return apiClient.get("/api/v1/users/me");
  },
  getSavedRoutes: async () => {
    // return apiClient.get("/api/v1/users/me/routes");
  }
};
