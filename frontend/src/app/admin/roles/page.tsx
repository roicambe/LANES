import UnderDevelopment from "@/features/admin/UnderDevelopment";

export default function RolesPage() {
  return (
    <UnderDevelopment
      sectionName="Roles"
      description="Role creation, permission management, and role assignment for sub-admins will be configured here."
    />
  );
}
