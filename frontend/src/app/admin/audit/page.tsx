import UnderDevelopment from "@/features/admin/UnderDevelopment";

export default function AuditTrailPage() {
  return (
    <UnderDevelopment
      sectionName="Audit Trail"
      description="A searchable, filterable log of all admin actions — including logins, approvals, and zone changes — will be displayed here."
    />
  );
}
